[Unit]
Description=egs

[Service]
User=nginx
ExecStart=/usr/bin/python3-gunicorn --bind unix:/var/egs/gunicorn.egs.sock --chdir=/opt/egs/egs/ egs.wsgi:application
StandardOutput=syslog
StandardError=syslog

[Install]
WantedBy=multi-user.target
